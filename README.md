
  # Create Purple Sun Landing Page

  This is a landing page for Purple Sun, built with React and Vite.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Coresense---Cora
